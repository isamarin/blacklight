{
	"name": "@blacklight/pages",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "cd ../desktop-tauri && pnpm build && rm -rf ../pages/public && mkdir -p ../pages/public && cp -r build/* ../pages/public/",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@blacklight/platform": "workspace:*",
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.9.3",
		"typescript": "^5.5.2",
		"vitest": "~4.1.9",
		"wrangler": "^4.92.0"
	}
}
