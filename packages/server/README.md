# @greenlight/server

This package is the server component. Is serves as a bridge between trpc and websocket.
Purpose of this package is so we can run the server while working on the web package. This package is also used in the desktop application to serve as a bridge to the backend.